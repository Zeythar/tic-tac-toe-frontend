# Backend API Configuration
# The base URL for the backend API (used for SignalR /gameHub endpoint)
# In development, this is proxied via proxy.conf.json to http://localhost:5109
# In production, set this to your deployed backend URL

# Development (using proxy):
API_BASE_URL=

# Production example (uncomment and set your backend URL):
# API_BASE_URL=https://api.yourdomain.com
