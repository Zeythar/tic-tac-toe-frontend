# Backend API Configuration
# 
# For DEVELOPMENT (local):
# Leave this empty - the app will use proxy.conf.json to connect to http://localhost:5109
#
# For PRODUCTION (Vercel):
# Set this as an environment variable in Vercel dashboard:
#   Variable: NG_APP_API_BASE_URL
#   Value: https://tic-tac-toe-api-aew1.onrender.com
#   (no trailing slash)
#
# The build script (scripts/set-env.js) will inject this into environment.prod.ts

NG_APP_API_BASE_URL=